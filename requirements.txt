# Phone Guardian uses only the Python standard library.
# All device functionality (GPS, camera, SMS, notifications) comes from
# the termux-api command-line tools, installed separately via:
#   pkg install termux-api
# and the companion Termux:API Android app (installed from F-Droid).
#
# This file is kept intentionally empty/documented in case you extend
# the project with extra pip packages later.
